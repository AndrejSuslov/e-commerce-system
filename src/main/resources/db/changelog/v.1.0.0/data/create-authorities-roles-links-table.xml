<databaseChangeLog xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd">

    <changeSet id="13" author="andrejsuslov308@gmail.com">
        <sql>
            CREATE TABLE IF NOT EXISTS authorities_roles_links (
                authority_id BIGSERIAL NOT NULL REFERENCES authorities(id),
                role_id BIGSERIAL NOT NULL REFERENCES roles(id)
            );
        </sql>
    </changeSet>

</databaseChangeLog>
